# Environment variables template for bash-skill

# MCP Server configuration
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=8000

# Default timeout for command execution (in seconds)
DEFAULT_TIMEOUT=30

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
